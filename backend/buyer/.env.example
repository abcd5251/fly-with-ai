# EVM Private Key (with 0x prefix)
# For testing, use a wallet with USDC on Base Sepolia testnet
# Get testnet USDC from: https://faucet.circle.com/
EVM_PRIVATE_KEY=0x...

# API endpoint (defaults to local seller server)
API_ENDPOINT=http://localhost:4021/weather
