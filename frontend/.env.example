# API URL for the backend server
VITE_API_URL=http://localhost:4021

# Demo private key for Base Sepolia testnet
# Generate a new key: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
# Fund it with testnet USDC from: https://faucet.circle.com/
VITE_DEMO_PRIVATE_KEY=0x...
