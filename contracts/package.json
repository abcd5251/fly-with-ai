{
  "name": "hold-escrow",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:hedera": "hardhat run scripts/deploy.cjs --network hederaTestnet",
    "deploy:base": "hardhat run scripts/deploy.cjs --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
